#include <iostream>
using namespace std;

int main() {
    double a, b, c;
    cout << "Ingrese a, b y c: ";
    cin >> a >> b >> c;

    double x1 = 0, x2 = 0;
    cout << "Soluciones: x1 = " << x1 << ", x2 = " << x2 << endl;

    return 0;
}

